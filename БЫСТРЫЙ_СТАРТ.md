# 🚀 Быстрый старт с системой ролей

## Для существующей базы данных

```bash
# 1. Активируйте виртуальное окружение
.\venv\Scripts\activate

# 2. Выполните миграции
python migrations/migrate_add_roles.py
python migrations/migrate_add_blocked.py
python migrations/migrate_add_tokens.py

# 3. Назначьте администратора
python migrations/make_admin.py

# 4. Запустите приложение
python run.py
```

## Для новой базы данных

```bash
# 1. Активируйте виртуальное окружение
.\venv\Scripts\activate

# 2. Запустите приложение (база создастся автоматически)
python run.py

# 3. Зарегистрируйте первого пользователя через веб-интерфейс

# 4. Назначьте его администратором
python migrations/make_admin.py
```

## Доступ к панели администратора

1. Откройте http://localhost:5000
2. Войдите как администратор
3. В меню выберите "Управление пользователями" 🛡️

## Управление ролями

**Путь:** Управление пользователями → Выбрать пользователя → Изменить роль

**Доступные роли:**
- 🔴 **Администратор** - полный доступ
- 🔵 **Аналитик** - доступ к аналитике
- 🟢 **Бухгалтер** - доступ к финансам
- ⚪ **Пользователь** - базовый доступ

## Управление токенами API

**Путь:** Меню пользователя → Настройки → Токены API

**Поддерживаемые маркетплейсы:**
- 🛍️ **Wildberries** - API токен
- 📦 **Ozon** - API токен + Client ID

**Возможности:**
- Добавление токенов маркетплейсов
- Редактирование токенов
- Безопасное хранение (скрыты по умолчанию)
- Удаление токенов

## Полезные команды

```bash
# Просмотр администраторов
python migrations/make_admin.py

# Повторная миграция (если нужно)
python migrations/migrate_add_roles.py

# Запуск приложения
python run.py
```

## Учетные данные по умолчанию

Если в вашей базе уже был пользователь:
- **Имя пользователя:** Admin
- **Email:** mveretelnikov@gmail.com
- **Роль:** Администратор (назначена автоматически)

## Возможные проблемы

❌ **Ошибка: "no such column: users.role"**
```bash
python migrations/migrate_add_roles.py
```

❌ **Ошибка: "no such table: tokens"**
```bash
python migrations/migrate_add_tokens.py
```

❌ **Нет администраторов в системе**
```bash
python migrations/make_admin.py
```

❌ **Модуль Flask не найден**
```bash
.\venv\Scripts\activate
pip install -r requirements.txt
```

## Дополнительная документация

- 📖 **Полная инструкция:** `README.md`
- 👥 **Система ролей:** `ИНСТРУКЦИЯ_ПО_РОЛЯМ.md`
- 🔑 **Управление токенами:** `ИНСТРУКЦИЯ_ПО_ТОКЕНАМ.md`
- 🛡️ **Блокировка пользователей:** `ИНСТРУКЦИЯ_ПО_БЛОКИРОВКЕ.md`
- 🧪 **Тестовый сценарий:** `TEST_SCENARIO.md`
- 📝 **Список изменений:** `ИЗМЕНЕНИЯ.md`

---

**Готово! Приятной работы! 🎉**

