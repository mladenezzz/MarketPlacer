# MarketPlacer - Веб-приложение с регистрацией и авторизацией

Современное веб-приложение на Python Flask с системой регистрации и авторизации пользователей.

## ✨ Возможности

- 🔐 **Безопасная авторизация** - Хеширование паролей с использованием Werkzeug
- 👤 **Регистрация пользователей** - Полная валидация форм с проверкой на уникальность
- 🎨 **Современный UI** - Адаптивный дизайн на Bootstrap 5
- 💾 **База данных** - SQLite для хранения данных пользователей
- 🔄 **Управление сессиями** - Flask-Login для безопасной работы с сессиями
- ✅ **Валидация форм** - WTForms с защитой от CSRF атак
- 📱 **Отзывчивый дизайн** - Работает на всех устройствах
- 👥 **Система ролей** - Управление правами доступа (Администратор, Аналитик, Бухгалтер, Пользователь)
- 🛡️ **Панель администратора** - Управление пользователями и их ролями

## 📋 Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## 🚀 Установка и запуск

### 1. Клонирование репозитория (или переход в директорию проекта)

```bash
cd MarketPlacer
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: **http://localhost:5000**

## 📁 Структура проекта

```
MarketPlacer/
│
├── app.py                  # Главный файл приложения
├── config.py               # Конфигурация приложения
├── models.py               # Модели базы данных
├── forms.py                # Формы регистрации, входа и управления ролями
├── migrate_add_roles.py    # Скрипт миграции для добавления ролей
├── requirements.txt        # Зависимости проекта
├── README.md              # Документация
│
├── templates/             # HTML шаблоны
│   ├── base.html          # Базовый шаблон
│   ├── index.html         # Главная страница
│   ├── register.html      # Страница регистрации
│   ├── login.html         # Страница входа
│   ├── dashboard.html     # Панель управления
│   ├── profile.html       # Профиль пользователя
│   ├── admin_users.html   # Управление пользователями (админ)
│   └── change_role.html   # Изменение роли пользователя (админ)
│
└── app.db                 # База данных SQLite (создается автоматически)
```

## 🎯 Использование

### Регистрация нового пользователя

1. Откройте http://localhost:5000
2. Нажмите "Регистрация"
3. Заполните форму:
   - Имя пользователя (минимум 3 символа)
   - Email (действительный адрес электронной почты)
   - Пароль (минимум 6 символов)
   - Подтверждение пароля
4. Нажмите "Зарегистрироваться"

### Вход в систему

1. Перейдите на страницу входа
2. Введите имя пользователя (или email) и пароль
3. Опционально: отметьте "Запомнить меня"
4. Нажмите "Войти"

### Доступные страницы

- **Главная** (`/`) - Приветственная страница с описанием возможностей
- **Регистрация** (`/register`) - Создание нового аккаунта
- **Вход** (`/login`) - Авторизация в системе
- **Панель управления** (`/dashboard`) - Личный кабинет пользователя (требует авторизации)
- **Профиль** (`/profile`) - Информация о пользователе (требует авторизации)
- **Управление пользователями** (`/admin/users`) - Панель администратора (только для администраторов)
- **Выход** (`/logout`) - Завершение сеанса

## 👥 Система ролей

В приложении реализована система управления ролями пользователей:

### Доступные роли

1. **Администратор** (admin)
   - Полный доступ ко всем функциям системы
   - Управление пользователями и их ролями
   - Доступ к панели администратора

2. **Аналитик** (analyst)
   - Доступ к аналитическим данным и отчетам
   - Расширенные права просмотра

3. **Бухгалтер** (accountant)
   - Доступ к финансовым данным
   - Управление бухгалтерскими операциями

4. **Пользователь** (user)
   - Базовые права доступа
   - Роль по умолчанию при регистрации

### Первый запуск с системой ролей

Если у вас уже есть существующая база данных, выполните миграцию:

```bash
python migrate_add_roles.py
```

Этот скрипт:
- Добавит поле роли к существующим пользователям
- Позволит назначить первого администратора
- Или создаст нового администратора, если пользователей нет

### Управление ролями

Только администраторы могут изменять роли пользователей:

1. Войдите в систему как администратор
2. Перейдите в "Управление пользователями" через меню
3. Выберите пользователя и нажмите "Изменить роль"
4. Выберите новую роль и сохраните изменения

**Примечание:** Администратор не может изменить свою собственную роль для безопасности.

## 🔒 Безопасность

Приложение включает следующие меры безопасности:

- Хеширование паролей с помощью `werkzeug.security`
- Защита от CSRF атак через Flask-WTF
- Безопасное управление сессиями с Flask-Login
- Валидация всех пользовательских данных
- HTTP-only cookies для сессий

## ⚙️ Конфигурация

Вы можете настроить приложение через переменные окружения:

- `SECRET_KEY` - Секретный ключ для сессий (обязательно измените в продакшене!)
- `DATABASE_URL` - URL базы данных (по умолчанию: SQLite)

**Пример для продакшена:**

```bash
export SECRET_KEY='your-very-secret-key-here'
export DATABASE_URL='postgresql://user:password@localhost/dbname'
```

## 🛠️ Разработка

### Режим отладки

По умолчанию приложение запускается в режиме отладки (`debug=True`). Для продакшена установите `debug=False` в файле `app.py`.

### База данных

База данных создается автоматически при первом запуске. Файл `app.db` будет создан в корневой директории проекта.

Для сброса базы данных просто удалите файл `app.db` и перезапустите приложение.

## 📦 Зависимости

- **Flask** - Веб-фреймворк
- **Flask-SQLAlchemy** - ORM для работы с базой данных
- **Flask-Login** - Управление сессиями пользователей
- **Flask-WTF** - Формы с защитой от CSRF
- **WTForms** - Валидация форм
- **email-validator** - Валидация email адресов
- **Werkzeug** - Утилиты для хеширования паролей

## 🤝 Вклад в проект

Если вы хотите улучшить проект:

1. Создайте форк репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект создан в образовательных целях и свободен для использования.

## 👨‍💻 Автор

Создано с ❤️ для демонстрации возможностей Flask

## 📞 Поддержка

Если у вас возникли проблемы или вопросы, пожалуйста, создайте issue в репозитории.

---

**Приятной работы с MarketPlacer! 🚀**

