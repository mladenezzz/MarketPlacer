# Инструкция по использованию системы ролей

## Что было добавлено

В приложение MarketPlacer добавлена система управления ролями пользователей с возможностью администратора изменять роли.

## Доступные роли

1. **Администратор (admin)** - полный доступ, управление пользователями
2. **Аналитик (analyst)** - доступ к аналитическим данным
3. **Бухгалтер (accountant)** - доступ к финансовым данным
4. **Пользователь (user)** - базовые права (по умолчанию)

## Первый запуск

### 1. Если база данных уже существует

Запустите скрипт миграции:
```bash
.\venv\Scripts\activate
python migrate_add_roles.py
```

Затем назначьте первого администратора:
```bash
python make_admin.py
```

### 2. Если база данных новая

Просто запустите приложение, зарегистрируйте пользователя и используйте скрипт `make_admin.py` для назначения администратора.

## Запуск приложения

```bash
.\venv\Scripts\activate
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

## Использование панели администратора

### Для входа как администратор:

1. Откройте http://localhost:5000/login
2. Войдите с учетными данными администратора
3. В навигационном меню появится пункт "Управление пользователями" (красная иконка)

### Управление ролями:

1. Перейдите в "Управление пользователями"
2. Вы увидите список всех пользователей с их текущими ролями
3. Нажмите "Изменить роль" у нужного пользователя
4. Выберите новую роль из выпадающего списка
5. Нажмите "Изменить роль" для сохранения

## Особенности

- **Администратор не может изменить свою собственную роль** для безопасности системы
- В панели управления ваша строка будет выделена синим цветом
- Роли отображаются цветными бейджами:
  - 🔴 Администратор (красный)
  - 🔵 Аналитик (синий)
  - 🟢 Бухгалтер (зеленый)
  - ⚪ Пользователь (серый)

## Просмотр своей роли

Ваша текущая роль отображается в профиле пользователя (раздел "Профиль" в меню).

## Новые файлы

- `models.py` - добавлено поле `role` и методы для проверки ролей
- `forms.py` - добавлена форма `ChangeRoleForm` для изменения ролей
- `app.py` - добавлены маршруты `/admin/users` и декоратор `admin_required`
- `templates/admin_users.html` - страница управления пользователями
- `templates/change_role.html` - страница изменения роли
- `migrate_add_roles.py` - скрипт миграции базы данных
- `make_admin.py` - скрипт для быстрого назначения администратора

## Разработка

При добавлении новых функций, требующих проверки ролей, используйте:

```python
# Проверка роли в маршруте
@app.route('/some-route')
@login_required
@admin_required  # Только для администраторов
def some_route():
    pass

# Проверка роли в коде
if current_user.is_admin():
    # Код для администратора
    pass

if current_user.is_analyst():
    # Код для аналитика
    pass

# В шаблонах
{% if current_user.is_admin() %}
    <!-- Контент только для администратора -->
{% endif %}
```

## Безопасность

- Все административные маршруты защищены декоратором `@admin_required`
- Администратор не может изменить свою собственную роль
- Изменения ролей логируются через flash-сообщения
- При попытке доступа к административным функциям без прав пользователь перенаправляется на дашборд

## Поддержка

Если возникли проблемы:

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что файл `app.db` существует
3. Убедитесь, что в базе данных есть хотя бы один администратор
4. При необходимости используйте скрипт `make_admin.py` для создания администратора

---

Приятной работы! 🚀

