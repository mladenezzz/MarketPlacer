# Инструкция по блокировке пользователей

## Что было добавлено

Добавлена функциональность блокировки/разблокировки пользователей администратором.

## Изменения в коде

### 1. Модель User (models.py)
- Добавлено поле `is_blocked` (Boolean) для хранения статуса блокировки
- Добавлен метод `is_active()` для интеграции с Flask-Login (заблокированные пользователи считаются неактивными)

### 2. Маршруты (app.py)
- **`/admin/users/<user_id>/block` (POST)** - блокировка пользователя
- **`/admin/users/<user_id>/unblock` (POST)** - разблокировка пользователя
- Обновлена логика входа: заблокированные пользователи не могут войти в систему

### 3. Интерфейс администратора (admin_users.html)
- Добавлена колонка "Статус" с визуальным отображением:
  - 🟢 Активен (зеленый бейдж)
  - 🔴 Заблокирован (красный бейдж)
- Добавлены кнопки блокировки/разблокировки для каждого пользователя
- Заблокированные пользователи выделяются красным фоном в таблице

### 4. Скрипт миграции (migrate_add_blocked.py)
- Автоматически добавляет поле `is_blocked` в существующую базу данных

## Как использовать

### Первый запуск (миграция базы данных)

Перед первым использованием функции блокировки необходимо обновить базу данных:

```bash
python migrate_add_blocked.py
```

### Блокировка пользователя

1. Войдите в систему как администратор
2. Перейдите в раздел "Управление пользователями" (`/admin/users`)
3. Найдите нужного пользователя в таблице
4. Нажмите кнопку с иконкой замка 🔒 (желтая кнопка)
5. Подтвердите действие в диалоговом окне

### Разблокировка пользователя

1. Войдите в систему как администратор
2. Перейдите в раздел "Управление пользователями" (`/admin/users`)
3. Найдите заблокированного пользователя (отмечен красным фоном)
4. Нажмите кнопку с иконкой разблокировки 🔓 (зеленая кнопка)
5. Подтвердите действие в диалоговом окне

## Ограничения

- Администратор не может заблокировать сам себя
- Заблокированный пользователь не сможет войти в систему
- При попытке входа заблокированного пользователя появится сообщение: "Ваш аккаунт заблокирован администратором. Обратитесь в поддержку."

## Визуальные индикаторы

| Статус | Цвет бейджа | Фон строки | Кнопка действия |
|--------|-------------|------------|-----------------|
| Активен | Зеленый | Обычный (белый) | 🔒 Заблокировать (желтая) |
| Заблокирован | Красный | Красноватый | 🔓 Разблокировать (зеленая) |

## Техническая информация

### Структура поля is_blocked
- Тип: Boolean
- По умолчанию: False (пользователь активен)
- NOT NULL: Да

### Безопасность
- Только администраторы имеют доступ к функциям блокировки/разблокировки
- Используется декоратор `@admin_required`
- Все действия требуют подтверждения от пользователя

