# Инструкция по управлению токенами API маркетплейсов

## Что было добавлено

Добавлена функциональность управления токенами API для интеграции с маркетплейсами Wildberries и Ozon.

## Возможности

- ✅ Добавление токенов Wildberries
- ✅ Добавление токенов и Client ID для Ozon
- ✅ Редактирование существующих токенов
- ✅ Удаление токенов
- ✅ Безопасное хранение токенов с возможностью скрытия/показа
- ✅ Доступ к токенам только для их владельца

## Первый запуск

### Для существующей базы данных

Перед первым использованием функции управления токенами необходимо обновить базу данных:

```bash
python migrate_add_tokens.py
```

Скрипт автоматически:
- Создаст таблицу `tokens` в базе данных
- Добавит необходимые индексы
- Настроит связь с таблицей пользователей

### Для новой базы данных

При первом запуске приложения таблица будет создана автоматически.

## Как использовать

### Доступ к управлению токенами

1. Войдите в систему
2. Нажмите на ваше имя в правом верхнем углу
3. В выпадающем меню выберите **Настройки** → **Токены API**

Или перейдите напрямую: `/settings/tokens`

### Добавление токена Wildberries

1. На странице токенов нажмите **"Добавить токен"**
2. Выберите маркетплейс: **Wildberries**
3. Вставьте ваш API токен
4. Нажмите **"Сохранить токен"**

**Где найти токен Wildberries:**
1. Войдите в личный кабинет продавца Wildberries
2. Перейдите в **Настройки** → **Доступ к API**
3. Создайте новый токен с нужными правами
4. Скопируйте токен (⚠️ показывается только один раз!)

### Добавление токена Ozon

1. На странице токенов нажмите **"Добавить токен"**
2. Выберите маркетплейс: **Ozon**
3. Вставьте ваш API токен
4. Введите Client ID
5. Нажмите **"Сохранить токен"**

**Где найти токен и Client ID Ozon:**
1. Войдите в личный кабинет продавца Ozon
2. Перейдите в **Настройки** → **Seller API**
3. Скопируйте **Client ID**
4. Создайте API-ключ и скопируйте его

### Редактирование токена

1. На странице токенов найдите нужный токен
2. Нажмите на кнопку с тремя точками (⋮)
3. Выберите **"Редактировать"**
4. Внесите изменения
5. Нажмите **"Сохранить токен"**

⚠️ **Внимание:** Изменение токена может повлиять на работу интеграций.

### Удаление токена

**Способ 1 (быстрый):**
1. На странице токенов найдите нужный токен
2. Нажмите на кнопку с тремя точками (⋮)
3. Выберите **"Удалить"**
4. Подтвердите удаление

**Способ 2 (из редактора):**
1. Откройте токен на редактирование
2. Прокрутите вниз
3. Нажмите **"Удалить токен"**
4. Подтвердите удаление

### Просмотр токена

По умолчанию токены скрыты для безопасности. Чтобы посмотреть токен:

1. На карточке токена найдите поле с токеном
2. Нажмите на иконку глаза (👁️)
3. Токен отобразится в открытом виде
4. Нажмите еще раз, чтобы скрыть

## Безопасность

- 🔒 Каждый пользователь видит только свои токены
- 🔒 Токены скрыты по умолчанию
- 🔒 Редактирование и удаление доступно только владельцу
- 🔒 При попытке доступа к чужому токену - ошибка доступа

## Структура данных

Каждый токен содержит:
- **Маркетплейс** - Wildberries или Ozon
- **API Token** - токен доступа к API
- **Client ID** - идентификатор клиента (только для Ozon)
- **Дата создания** - когда токен был добавлен
- **Дата обновления** - когда токен был изменен (если был)

## Изменения в коде

### 1. Модель Token (models.py)
- Добавлена модель `Token` для хранения токенов
- Связь один-ко-многим с пользователем
- Каскадное удаление токенов при удалении пользователя

### 2. Формы (forms.py)
- Добавлена форма `TokenForm` для добавления/редактирования токенов
- Валидация данных
- Условное отображение поля Client ID

### 3. Маршруты (app.py)
Добавлены новые маршруты:
- **`/settings/tokens`** (GET) - список токенов пользователя
- **`/settings/tokens/add`** (GET, POST) - добавление токена
- **`/settings/tokens/<id>/edit`** (GET, POST) - редактирование токена
- **`/settings/tokens/<id>/delete`** (POST) - удаление токена

### 4. Шаблоны
- **`tokens.html`** - список токенов с карточками
- **`add_token.html`** - форма добавления токена
- **`edit_token.html`** - форма редактирования токена

### 5. Навигация (base.html)
- Обновлено меню пользователя
- Добавлен раздел "Настройки" с подпунктом "Токены API"

## Техническая информация

### Структура таблицы tokens

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Первичный ключ |
| user_id | INTEGER | ID пользователя (внешний ключ) |
| marketplace | VARCHAR(50) | Название маркетплейса |
| token | VARCHAR(500) | API токен |
| client_id | VARCHAR(200) | Client ID (опционально) |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

### Индексы

- `idx_tokens_user_id` - для быстрого поиска по пользователю
- `idx_tokens_marketplace` - для фильтрации по маркетплейсу

## Возможные проблемы

### ❌ Ошибка: "no such table: tokens"

**Решение:**
```bash
python migrate_add_tokens.py
```

### ❌ Не могу добавить токен Ozon без Client ID

**Решение:** Client ID обязателен для Ozon. Найдите его в настройках API в личном кабинете продавца.

### ❌ Токен не отображается после добавления

**Решение:** Обновите страницу. Если проблема сохраняется, проверьте консоль браузера на наличие ошибок.

## Дальнейшее развитие

Планируется добавить:
- 🔄 Проверка валидности токенов
- 📊 Использование токенов для получения статистики
- 🔔 Уведомления об истечении токенов
- 📈 Интеграция с API маркетплейсов

## Поддержка

Если возникли проблемы:

1. Убедитесь, что выполнена миграция базы данных
2. Проверьте, что вы авторизованы в системе
3. Убедитесь, что токены корректны
4. При необходимости создайте issue в репозитории

---

**Приятной работы с токенами! 🔑**

